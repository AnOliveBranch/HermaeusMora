// Copy this file to config.json and remove all "comments"
{
    "discordToken": "", // Discord token for the bot
    "guildId": "", // Guild ID for deploying guild-specific commands
    "clientId": "", // Application ID found in discord dev portal
    "logInfo": "", // "true" or "false" if informational messages should be logged to another discord channel
    "logErrors": "", // "true" or "false" if errors should be logged to another discord channel
    "infoLogChannel": "", // Channel ID for logging info, if enabled
    "errorLogChannel": "", // Channel ID for logging errors, if enabled
}